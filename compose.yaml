services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: yoga_backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app  # for dev